{
  "enabledPlugins": {},
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json; d=json.load(sys.stdin); fp=d.get('tool_input',{}).get('file_path',''); print('[super-professor] plugin.json alterado — atualize version em installed_plugins.json se mudou a versão.') if '.claude-plugin/plugin.json' in fp else None\""
          }
        ]
      }
    ]
  }
}
